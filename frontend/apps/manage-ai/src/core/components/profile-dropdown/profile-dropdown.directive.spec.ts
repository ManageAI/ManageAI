import { ProfileDropdownDirective } from './profile-dropdown.directive';

describe('ProfileDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
